// import {createSelector} from 'reselect'


// //selector
// export const getImage = (state) => state.user.image;
// export const getName = (state) => state.user.full_name;

// //reselect function
// export const makeGetUserState = () => createSelector(
//     [ getImage, getName ],
//     (image) => image,
//     (full_name) => full_name,
   
//     )
    
//     export default makeGetUserState



